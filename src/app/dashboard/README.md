# Live Dashboard V2

Dashboard V2 is mounted as a safe runtime layer over the existing dashboard. It reads the current business-scoped local data, adapts metrics and quick actions to the saved business type, and delegates navigation to the existing modules.

The original dashboard remains in the application as a fallback and its data/business logic is not deleted.
